# Include the backend services
include:
  - path: ../../integration-test/resources/compose-backends.yml

services:
  # ... dockerized microservice
  sensor-ingestion:
    build:
      context: ../../..
      dockerfile: src/main/docker/Dockerfile.jvm
    image: camelbee/sensor-ingestion
    ports:
      - 8080:8080
    restart: unless-stopped
    environment:
      MQTT_URL: tcp://mosquitto:1883
      MONGODB_URI: mongodb://root:example@mongodb:27017/camelbee?authSource=admin
      MONGODB_DATABASE: camelbee

      # Kafka
      KAFKA_URL: broker:29092
      JAVA_OPTS: >
        -XshowSettings:vm
        -XX:MetaspaceSize=512m
        -XX:MaxMetaspaceSize=1024m
        -XX:InitialRAMPercentage=70
        -XX:MaxRAMPercentage=70
        -XX:MinRAMPercentage=70
        -XX:+UseG1GC
        -XX:MaxGCPauseMillis=200
        -XX:+UseStringDeduplication
        -Dcamel.threads.virtual.enabled=true
        -Dquarkus.http.host=0.0.0.0
        -Djava.util.logging.manager=org.jboss.logmanager.LogManager
    depends_on:
      broker:
        condition: service_healthy
      mosquitto:
        condition: service_started
      mongodb:
        condition: service_started
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 4G
    healthcheck:
      # Use liveness endpoint: Camel readiness probes block the vert.x event loop
      test: ["CMD", "curl", "-f", "http://localhost:8080/health/live"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s

networks:
  default:
    name: camelbee_default_network
    driver: bridge
