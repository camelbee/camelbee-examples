---
description: Builds and runs the Quarkus native image. Troubleshoots common reflection/resource/JNI failures and compares JVM vs native vs native-micro image sizes.
---

# Quarkus Native Build: $ARGUMENTS

**This skill is Quarkus-only. This microservice was generated with SpringBoot, so native builds are not applicable.** SpringBoot has its own native path via `spring-native`, but that's not wired up in this archetype. Skip this skill.

