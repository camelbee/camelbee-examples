# loan-application-service

You are an expert Apache Camel developer working on a **CamelBee-generated microservice** (SPRINGBOOT). This project was generated by the [CamelBee MCP Server](https://www.camelbee.io/mcp) and is a **fully working microservice out of the box** — all tests pass, all dependencies are correct. Treat existing code as the source of truth.


- **Framework:** SPRINGBOOT with Apache Camel 4.x
- **Language:** Java 21
- **Build:** Maven (`./mvnw`)
- **DI:** Spring (Lombok `@RequiredArgsConstructor`)
- **Mapping:** MapStruct
- **Testing:** JUnit 5, Camel Test, TestContainers, WireMock, RestAssured, k6 (load)
- **Containers:** Docker Compose
- **Observability:** CamelBee Tracer + Debugger UI, Logback with Logstash JSON encoder, MDC correlation
- **Database:** Multi-vendor — PostgreSQL, MySQL, MariaDB, Oracle, SQL Server, DB2 (switchable via `DB_VENDOR`)
- **Cache:** Write-through cache via `routes/producer/cache/` — vendor **Redis** (`camel-spring-redis`). JSON values only. Operations: CRO/CRB/REO/UPO/DEO (no LSO/GEO — read paths need cache-aside, not fan-out).


3-layer route architecture: **Consumer → Central → Producer**

| Layer | Location | Role |
|-------|----------|------|
| Consumer | `routes/consumer/{technology}/` | Entry points — unmarshal, validate, map API→Domain |
| Central | `routes/central/` | Orchestration — fan-out to backends |
| Producer | `routes/producer/{technology}/` | Outgoing — map Domain→Infra, call backend |

**Models:** `model/api/` (API) → `model/domain/` (internal) → `model/infra/` (backend)
**Mappers:** `mapper/api/` (API↔Domain), `mapper/infra/` (Domain↔Infra) — MapStruct


- REST
- MCP
- KAFKA


- KAFKA
- JPA
- REST
- CACHE


```bash
./mvnw clean package                    # Build
./mvnw test                             # Unit tests
./mvnw jacoco:prepare-agent failsafe:integration-test failsafe:verify  # Integration tests
./mvnw verify -Pblack-box-test          # Black-box tests
./mvnw generate-sources                 # Regenerate models from specs
docker compose up --build               # Run with all backends
```


This microservice includes the **CamelBee embedded debugger UI** at `http://localhost:8080/camelbee/index.html` (available when the app is running via `docker compose up`). It provides route topology visualization, real-time message tracing, request/response inspection, and health/metrics monitoring. Use `/cb-debug` to trigger a request and verify message flow visually.


This project includes a `.mcp.json` file that registers the **CamelBee MCP Server** at `https://www.camelbee.io/mcp`. The MCP server is available only for **paid subscriptions**. To use it:

1. Get an API key from [camelbee.io](http://www.camelbee.io) (requires a paid subscription)
2. Replace `YOUR_CAMELBEE_API_KEY` in `.mcp.json` (Claude Code / OpenCode) or `.cursor/mcp.json` (Cursor) with your actual API key

Once configured, you can use the MCP server to **generate reference microservices on-demand**.

**How it works with `/cb-add-interface` and `/cb-add-backend`:**

When you need to add a new interface or backend that wasn't included in the original project generation:
1. **Call the CamelBee MCP server** to generate a reference project that includes the desired technology (with the same framework, service name, and existing interfaces/backends plus the new one)
2. **Inspect the generated reference code** — study the consumer routes (`routes/consumer/{technology}/`), producer routes (`routes/producer/{technology}/`), models, mappers, configuration (`application.yml`), Docker services, integration tests (`itest/{technology}/`), and black-box tests (`bbtest/{technology}/`)
3. **Copy the relevant pieces** into the current project following the checklist in the `/cb-add-interface` or `/cb-add-backend` skill
4. **Adapt to current domain** — replace Order/Purchase references with your domain models
5. **Delete the reference project** after copying

The `/cb-add-interface` and `/cb-add-backend` skills will guide you through the full process.


**MUST preserve** (breaking these breaks CamelBee observability):
- `camelBeeRouteConfigurer.configureRoute(this)` — first call in every route's `configure()`
- `errorHandler(genericExceptionHandler.appErrorHandler())` — consumer routes only
- `errorHandler(noErrorHandler())` — central and producer routes
- Exchange properties: `Constants.ORIGINAL_BODY`, `Constants.ACTUAL_RESPONSE_BODY`, `Constants.ORIGINAL_ACCEPT_CONTENT_TYPE`
- `.routeId()` on every route, `.id()` on every backend endpoint in central routes
- 3-layer flow: Consumer → Central → Producer (never call backend from consumer directly)
- **`@ToString` on ALL model classes (api / domain / infra):** The CamelBee tracer and debugger UI call `toString()` on every exchange body to display message content. Without a proper `toString()`, every message in the UI and in logs appears as `ClassName@abc123` — completely unreadable. This applies to all three model layers:
  - `model/api/` — API-facing models (JSON/XML POJOs, event wrappers, CSV records)
  - `model/domain/` — core domain objects (`Order`, `OrderItem`, `Error`, event types)
  - `model/infra/` — backend-specific models (`Purchase`, `PurchaseItem`, etc. for every backend)

  **Generated templates already include `@ToString`.** If you add a new model class manually, always include it — use Lombok `@ToString` as the first choice. For JPA entities with bidirectional relationships, use `@ToString(exclude = "fieldName")` to avoid infinite recursion: exclude the `@ManyToOne` back-reference from `PurchaseItem` (`exclude = "purchase"`) and optionally exclude the `@OneToMany` collection from `Purchase` (`exclude = "items"`). Models code-generated by Maven plugins (OpenAPI Generator, Protobuf, Avro, JAXB) already have `toString()` from their generators — no action needed for those. This rule does NOT apply to `String`, `byte[]`, or streaming types.

**Do NOT:**
- Change dependency/plugin versions unless explicitly asked
- Modify TestContainers, Docker Compose, or test infrastructure unless broken by your change
- Add workarounds for "compatibility issues" — the generated code works as-is
- Restructure the project layout or build configuration
- Skip success scenarios in tests — every new feature MUST have both success AND error tests

**Testing requirements (MANDATORY):** Every new operation or feature must include:
- **Unit tests:** at least 1 success + 1 error scenario (AdviceWith mocks)
- **Integration tests:** at least 1 success scenario with real backend data verification + 1 error scenario. Do NOT only write error tests — set up the backend test data (DB records, WireMock stubs, queue messages) needed for success scenarios
- **Black-box tests:** at least 1 success scenario with backend data verification + 1 error scenario. Do NOT skip success tests because backend setup is complex — study existing tests for how to set up test data

**Running tests:** After implementing code and tests, ask the developer if they want you to run the tests. If yes, run them yourself — do not ask the developer to run them manually. Run in order: unit tests first (`./mvnw test`), then integration tests (`./mvnw jacoco:prepare-agent failsafe:integration-test failsafe:verify`), then black-box tests (`./mvnw verify -Pblack-box-test`). If any test fails, fix the issue and re-run.

**Backend credential / connection alignment (MANDATORY):** Every backend has the **same credential repeated in 3–4 places**. If you change a username, password, host, port, database name, keyspace, collection, topic, queue, bucket, table, or cache name in one place, you MUST update every other place it appears — mismatches surface as `authentication failed`, `Unable to retrieve password for "X"`, `connection refused`, `unknown host`, or silent test failures where `expectedMessageCount(0)` is never violated.

The four touchpoints for every backend:

1. **Docker server config** — `src/integration-test/resources/compose-backends.yml` defines the backend service (env vars like `USER`, `PASS`, `MONGO_INITDB_ROOT_PASSWORD`, `POSTGRES_USER`). This is where the backend container learns what credentials to accept.
2. **App runtime config** — `src/main/resources/application.yml`. The running app reads these when it connects to the backend.
3. **Black-box app env overrides** — `src/black-box-test/resources/compose-blackbox.yml` and `compose-blackbox-native.yml`. When the app runs inside Docker Compose for bbtests, these env vars override `application.yml` (because the app talks to the backend via its Docker service name, e.g. `cache:11222`, not `localhost`).
4. **Test-JVM clients** — `src/black-box-test/java/utils/DataVerifier.java`, `DataSeeder.java`, `MessageVerifier.java`, and integration-test `@BeforeAll` / dynamic route setups. These run in the **Maven JVM on the host**, not inside Docker, so they connect to the **published localhost port** (e.g. `localhost:11222`) with the **same credentials** the server was provisioned with.

When any of the four drifts from the others, tests fail — and often with a message that looks like a bug somewhere else. Example: Infinispan with `USER: camelbee / PASS: camelbee` in `compose-backends.yml` but `username("admin").password("admin")` in `DataVerifier.java` throws `ELY05161: Unable to retrieve password for "admin"` — the server has no `admin` user, the test client was just wrong.

Grep pattern to verify alignment before committing: pick one credential (e.g., `camelbee` for Infinispan, `secret` for Postgres, `example` for MongoDB root) and `grep -rn <credential>` across `src/main/resources`, `src/integration-test/resources`, `src/black-box-test/resources`, and `src/black-box-test/java/utils/`. Every hit must agree.

See `.claude/rules/test-patterns.md` (Claude Code) or `.cursor/rules/test-patterns.mdc` (Cursor) § "Backend credential & connection alignment" for the full matrix per backend.

**If something breaks after your change**, fix your change — don't blame the infrastructure. Before experimenting, diff the affected file against the baseline scaffold to see exactly what drifted from the known-good state:

```bash
git diff camelbee-baseline -- <path/to/file>          # what changed vs. the working scaffold
git show camelbee-baseline:<path/to/file>             # the original working version of the file
git diff --stat camelbee-baseline                     # everything that has changed since generation
```

The `camelbee-baseline` tag is created in "Step 0" below — it captures the fully-working scaffold exactly as CamelBee generated it. When a route, test, compose service, or plugin misbehaves, the baseline is the ground truth for what a working version of that file looks like for this exact interface/backend/framework combination. Consult it **before** speculative fixes.


These models are **generated at build time** by Maven plugins. Edit the spec file, not the Java output. Run `./mvnw generate-sources` to regenerate.

| Plugin | Spec (edit this) | Generated Output (do NOT edit) |
|---|---|---|
| **OpenAPI Generator** | `src/main/resources/openapi/order-api.yaml` | `model/api/json/`, `model/api/xml/` (POJOs with validation annotations) |
| **OpenAPI Generator** | `src/main/resources/backends/openapi/purchase-api.yaml` | `model/infra/json/` (backend response models) |
| **Avro Plugin** | `src/main/avro/*.avsc` | `model/api/avro/`, `model/infra/avro/` (Avro Java classes) |

**Key points:**
- All generated code goes to `target/generated-sources/` — never commit this directory
- To change a model: edit the spec file (YAML/XSD/proto/avsc/WSDL), then `./mvnw generate-sources`
- See `.claude/rules/build-plugins.md` for full plugin details (loaded when editing `pom.xml`)


- **Main config:** `src/main/resources/application.yml`
- **Spring profiles:** `dev`, `test`, `itest`, `prod` (activate via `spring.profiles.active` or `SPRING_PROFILES_ACTIVE`)
- **Database vendor:** switchable via `${DB_VENDOR:postgresql}` env var — supported: postgresql, mysql, mariadb, oracle, mssql, db2
- **Flyway:** auto-migration per vendor (`src/integration-test/resources/docker/jdbc/{vendor}/`)
- **Logging:** Logback with Logstash JSON encoder for prod, MDC fields for correlation (`businessTransactionId`, `tx_request_uri`, etc.)


| File | Purpose |
|---|---|
| `src/main/resources/application.yml` | Application configuration (all properties) |
| `src/main/resources/openapi/order-api.yaml` | OpenAPI spec (REST API models) |
| `src/main/resources/logback-spring.xml` | Logging configuration (profiles + Logstash) |
| `docker-compose.yml` | Full app + all backends |
| `src/integration-test/resources/compose-backends.yml` | Backend services for integration tests |
| `src/black-box-test/resources/compose-blackbox.yml` | Black-box test Docker Compose |
| `src/integration-test/resources/docker/wiremock/` | WireMock stubs for REST/SOAP/gRPC backends |
| `config/` | `SharedMapperConfig`, SOAP/AMQP/RabbitMQ configs |
| `docs/postman/` | Postman collection for REST API testing |
| `docs/k6/` | k6 load test scripts (REST, gRPC, GraphQL, SOAP, WebSocket, MCP) — run via `/cb-load-test` |
| `src/main/docker/Dockerfile.*` | JVM and legacy-jar Dockerfiles |


| Code | Operation |
|---|---|
| CRO | CreateOrder |
| LSO | ListOrders |
| GEO | GetOrder |
| REO | ReplaceOrder |
| UPO | UpdateOrder |
| DEO | DeleteOrder |
| CRB | CreateOrdersBatch |


The Order/Purchase code is a **working reference implementation** meant to be replaced with your domain.


**Step 0 — Tag the baseline scaffold (do this FIRST, before any changes):**
```bash
# If this directory is not yet a git repo:
git init && git add -A && git commit -m "chore: initial camelbee scaffold"

# Then tag the current HEAD as the baseline (safe to re-run; -f overwrites an older tag):
git tag -f camelbee-baseline
```

This creates a **named, stable reference** to the fully-working scaffold exactly as CamelBee generated it. The tag survives any number of subsequent commits, so you can always reach the original working version of any file — even months later, after hundreds of changes.

Use it as a troubleshooting anchor throughout the project's life:

| Situation | Command |
|---|---|
| "This test used to pass — what did I change?" | `git diff camelbee-baseline -- src/integration-test/...` |
| "What did the original producer route look like?" | `git show camelbee-baseline:src/main/java/.../producer/.../XyzProducerRoute.java` |
| "Which files have I touched since generation?" | `git diff --stat camelbee-baseline` |
| "Did I break the Docker compose healthchecks?" | `git diff camelbee-baseline -- src/integration-test/resources/compose-backends.yml` |

Do NOT delete this tag. It's cheap (one pointer), and it's the single highest-leverage debugging tool in this repository — the generated scaffold is CI-validated by CamelBee, so any divergence between your broken file and the baseline is almost certainly the cause.

**Step 1 — Implement new functionality:**
- Study existing code for the same operation type (e.g., look at CreateOrder when adding a new write operation)
- Follow the same patterns exactly — same structure, same error handling, same test approach
- Use `/cb-add-operation` to add new operations to existing routes
- Use `/cb-add-interface` or `/cb-add-backend` to add new technologies — these use the CamelBee MCP server (preferred) a reference project, then guide you through copying the relevant pieces

**Step 2 — Create ALL three test levels (MANDATORY — do NOT skip any):**
Every new operation or domain MUST have all three test levels. Plan these as separate tasks:
1. **Unit tests** (`src/test/`) — at least 1 success + 1 error scenario per operation (AdviceWith mocks)
2. **Integration tests** (`src/integration-test/`) — at least 1 success + 1 error scenario with real backends (TestContainers)
3. **Black-box tests** (`src/black-box-test/`) — at least 1 success + 1 error scenario, fully dockerized end-to-end (Docker Compose). Study existing black-box tests in `src/black-box-test/` for patterns — they use RestAssured/protocol clients against the running application container. See `.claude/rules/test-patterns.md` (Claude Code) / `.cursor/rules/test-patterns.mdc` (Cursor) for full details.

**Step 3 — Run ALL tests and fix issues:**
Run in order: `./mvnw test` → `./mvnw jacoco:prepare-agent failsafe:integration-test failsafe:verify` → `./mvnw verify -Pblack-box-test`. Fix any failures before proceeding.

**Step 4 — Clean up reference code (after your code works and ALL tests pass):**
Once your domain operations are implemented and all tests pass, remove the original Order/Purchase code. **Use the `/cb-replace-domain` skill** for a guided walkthrough — it enumerates every file and test that references Order/Purchase so nothing is missed.

Broad buckets (see the skill for the exact file list):
- Routes: consumer, central, and producer route classes
- Models: Order/OrderItem (api), Purchase/PurchaseItem (domain and infra)
- Mappers: OrderMapper, PurchaseMapper and their tests
- Tests: unit, integration, and black-box tests for Order/Purchase
- Specs: update or replace OpenAPI/proto/avro/wsdl specs — run `/cb-regen` after editing
- WireMock stubs, Docker init scripts (SQL, CQL, etc.) for the old domain
- **Keep the infrastructure**: build config, Docker Compose services, TestContainers setup, CamelBee config, error handling framework


Detailed rules load automatically when you work on matching files. The same content is available to every supported AI tool under its own conventions:

| Tool | Root instructions | Path-scoped rules | Slash commands | MCP config |
|---|---|---|---|---|
| Claude Code | `CLAUDE.md` | `.claude/rules/*.md` | `.claude/skills/{name}/SKILL.md` | `.mcp.json` |
| Cursor | `AGENTS.md` | `.cursor/rules/*.mdc` | — (no repo-level commands) | `.cursor/mcp.json` |
| OpenCode | `AGENTS.md` | — (reads AGENTS.md only) | `.opencode/command/*.md` | `.mcp.json` |
| Codex CLI | `AGENTS.md` | — | — (prompts are user-level) | user-level config |
| GitHub Copilot | `.github/copilot-instructions.md` | `.github/instructions/*.md` | — | via VS Code settings |

Rule files (same topics, different frontmatter per tool; generated in lockstep):
- `route-architecture` — 3-layer architecture, model tiers, error handling, all protocol patterns
- `camel-dsl-patterns` — Camel DSL code examples per technology, marshaling, content negotiation
- `test-patterns` — Unit/integration/black-box test patterns, mocking, assertions, test data
- `build-plugins` — Maven plugin details (code generation, XML replacer, descriptor copy)
- `observability-patterns` — CamelBee tracer, debugger UI, MDC correlation, logging, error framework
- `security-patterns` — OAuth2/JWT, Keycloak realm, JWKS caching, role/scope checks, test tokens
- `db-migration-patterns` — Flyway migrations, per-vendor quirks, DataSeeder reset scripts, multi-datasource config

Slash commands (available in Claude Code + OpenCode — same body, different directory):
- `/cb-add-interface`, `/cb-add-backend`, `/cb-add-operation` — scaffold new technologies / operations via the CamelBee MCP server
- `/cb-replace-domain` — guided walkthrough for replacing the Order/Purchase reference domain with your own
- `/cb-regen` — regenerate spec-driven code (`./mvnw generate-sources`) after editing YAML/XSD/proto/avsc/WSDL
- `/cb-load-test` — run k6 scripts in `docs/k6/` against the running microservice
- `/cb-add-migration` — add a new Flyway migration (`V{n}__{description}.sql`) for SQL/JPA databases
- `/cb-review` — code review of pending changes
- `/cb-run-tests` — run unit / integration / black-box tests
- `/cb-coverage` — coverage report
- `/cb-debug` — trigger a traced request through the CamelBee debugger UI

Subagents (Claude Code):
- `code-reviewer` — independent review using a different model for perspective
- `test-writer` — writes missing tests for existing code (unit + integration + black-box) following the existing patterns